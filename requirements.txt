# FlexiChrono Backend Python Dependencies
# P0: pyyaml (character config), bcrypt (password hashing)
# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
# HTTP Client (internal services + external APIs)
httpx>=0.25.0
# Data Validation
pydantic>=2.0.0
# P0: YAML config loader for characters/*.yaml
pyyaml>=6.0
# P0: bcrypt password hashing for UserDB
bcrypt>=4.0
# TTS (edge-tts free Microsoft TTS)
edge-tts>=6.1.0
# Async HTTP (edge-tts dependency, explicit for version compat)
aiohttp>=3.9.0
# Chinese tokenizer (optional, MemoryDecaySystem, auto-fallback to 2-gram if missing)
jieba>=0.42.1
