# FlexiChrono 需求清单（开发手册 v0）

> 用法：这份清单是"我提问、你回答、再出成品"流程的基准。
> 【已完成】= 代码已上线；【待验收】= 改完等你实测；【待做】= 还没动；【待拍板】= 有歧义，等你回答后才动手。

## 一、当前线上架构（已跑通，勿动）
- 后端五个进程在 Railway：main(8000) / vector(8001) / personality(8002) / proactive(8003) / voice(8004)。
- 前端：GitHub → Cloudflare Pages = nianqi.online；管理后台 nianqi.online/admin.html；私有接口 jinwen.online。
- Live2D 与静态资源在 Cloudflare R2 = r2.jinwen.online。
- QQ：阿里云 47.97.30.194:4310 跑 NapCat。
- 三角色音色：念琦=ICL_uranus_甜妹娇俏(seed-tts-2.0)；清禾=Sophie(2.0)；璟雯=元气女友moon(1.0，待换2.0)。

## 二、已完成（已交付）
- 网页端流式回复改成"一句一条气泡 + 句间停顿约1秒"。
- 时间戳灰条中途消失修复（AI 开口前也按五分钟阈值插条）。
- 语音消息微信化：只显示语音条，长按/右键转文字，TTS 失败退回文字。
- 通话人物 Live2D 变形修复（ResizeObserver 等比缩放）。
- 语音情绪透传：人格返回情绪标签 → 翻译成语气指令喂给 2.0 TTS。
- ASR 从阿里云 NLS 换成火山豆包大模型识别。
- QQ ↔ 网页跨端消息同步。

## 三、待验收（代码已改，等你实测）
- 逐句气泡 + 时间条效果（推 GitHub → Cloudflare 构建 → Ctrl+F5）。
- 语音情绪语气是否随情绪变化（看 Railway 日志"本次情绪="）。

## 四、待做（按优先级）

### P0 — 影响架构/主线体验
1. 双线时间线（模式一/模式二）——【已拍板方向，待细节确认】：
   - 模式一（主线/现在时）：她在平行世界里，从小一起长大的朋友分开之后，遇见用户，这是和用户关系发展的当下故事。
   - 模式二（前史/过去时）：把她"和朋友分开之前"的那段前史展现给用户，让用户代入/阅读。【已拍板：纯观看剧情，不能对话，不动主线好感度/记忆】
2. 管理员后台三级层级：用户 → 时间线 → 角色 → 好感度/数值；日程表进管理员后台，实时刷新；普通用户看不到管理员功能。
3. 角色日程表——【核心已上线，本次补完管理员搜索】：
   - 现状：主课表=role_schedule 表（按角色+六个时段活动池，admin"日程管理"页可改，循环注入她说话状态）；个人约定=character_appointments 表（按 user_id 隔离，自动提取带时间地点的约定，delete_after=发生后一周自动清，admin"约定/待办"页可增删改）。
   - 本次新增：admin 约定页顶部加"用户名模糊搜索"框，后端 /api/admin/appointments 加 q 参数 LIKE user_id，列表显示 👤用户名。
   - 待做：大学生课表"有时上课有时没课"的随机感（当前六个时段是静态活动池，未做每周随机排课）。
4. 安全清单：base64 明文 token、admin123 默认密码、CORS *、验证码爆破、set_user_field 可改 is_admin、内部 token 弱默认、token 永不过期。

### P1 — 提升真人感
5. 情绪惯性 / 独立生活模拟 / 记忆情绪标签（对标聆屿AI：沉默感知、情境感知、话题延续）。
6. Live2D 鼠标互动：摸头按好感度分档反应——【暂缓，用户决定后面单独开模块做】。前提待定：平时主界面是否常驻人物（见下方"待确认"）。
7. 网页实时语音通话（微信式，无字幕）：接通后界面切成通话画面，人物出现。
8. 平时人物只显示上半身偏小、手机端人物居中；输入时人物加高斯模糊。

### P2 — 内容与体验
9. 天气/时效兜底：先 Kimi 搜索，失败走火山豆包搜索；模型先判断是否时效再决定搜不搜；超时10秒；成本护栏。
10. 角色地址写死防漂移。
11. 唱歌：AI 上网搜歌词念/唱一段高潮（注意：TTS 唱不了旋律，要么念歌词、要么接入唱的模型）。
12. 后端把语音音频落 R2，支持刷新后语音可播放。
13. 清禾/璟雯 YAML 补 speech_examples。
14. 管理员/用户体系：QQ邮箱注册登录、密码加密、改密码需旧密码、邮箱找回、QQ自动注册发临时登录链接。

### P3 — 工程债
15. main.py 近四千行拆 routes/、补单测、加 CI、structlog、Sentry、.gitignore。
16. UGC 自创角色（法律风险高，最后再做）。

## 五、待你拍板的问题
1. ~~双线是什么~~ —— 已答：模式一=相遇后的主线，模式二=前史阅读线。待补：模式二能不能对话、是否动主线数据。
2. 主动消息频率 —— 【用户说再想想，暂缓】。
3. ~~日程表~~ —— 已答：主课表全局循环+每用户个人表叠加，约定一周后清除。
4. ~~摸头分档~~ —— 已答：暂缓，后面单独做。
5. 先做哪块 —— 待答。

- 人物显示【已拍板】：平时聊天界面不显示 Live2D 人物，仅语音通话界面显示；其他展示位置以后再加，当前不做。
